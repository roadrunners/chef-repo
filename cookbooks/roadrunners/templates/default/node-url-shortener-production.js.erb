module.exports = {
  db: {
    username: "<%= @db_username %>",
    password: "<%= @db_password %>",
    database: "<%= @db_name %>",
    host: "<%= @db_host %>",
    logging: false
  },
  redis: {
    host: "<%= @redis_host %>"
  }
}
